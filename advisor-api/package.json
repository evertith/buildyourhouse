{
  "name": "buildyourhouse-advisor-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute buildyourhouse-advisor --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute buildyourhouse-advisor --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}
